?start : claim ":" type_definition
type_definition : struct_definition | basic_definition | interface_definition
struct_definition : "struct" "{" struct_defbody "}"
struct_defbody : start*

string : ESCAPED_STRING
typename : /^[a-zA-Z_]+[\w]*/

%import common.ESCAPED_STRING
%import common.SIGNED_NUMBER
%import common.WS
%ignore WS